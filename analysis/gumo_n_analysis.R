# gumo_n_analysis.R
## script for analyzing the gumo data for changes in leaf n with elevation

## read in data
gumo_n_data <- read.csv("../data/gumo_n_data.csv")
head(gumo_n_data)

## examine nitrogen-elevation trends

### plot leaf nitrogen against elevation
# plot(n_weight_p ~ )


#####################
#### to do
#####################
# feb 13, 2024: get elevation data into main dataset, plot N as a function of elevation, 
# fit linear model of n as a function of elevation